import {ProjectTileContainer} from "../../styles/components/projects/ProjectTile.css";

const ProjectTile = ({image, projectName, projectContent, onClick, style}) => {
  return (
    <ProjectTileContainer style={style}>

    </ProjectTileContainer>
  )
}

export default ProjectTile;